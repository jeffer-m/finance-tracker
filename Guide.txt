Use virtual environments for this project.

commands:
    python -m venv venv  
    venv\Scripts\activate  
    pip install -r requirements.txt

    // to run the project
    uvicorn main:app --reload

    // to deactivate venv
    deactivate

if you add any more packages, include them in requirements.exe

